# A03 MyOS— 设计思路

## 这个软件要解决什么问题？

现在我们的流程是：**改参数 → 改 YAML 文件 → 杀节点 → 重新启动**，非常麻烦。

我们希望有一个软件，能**在车子跑的时候，拖动滑块就能调参数，立刻生效**。

***

## 软件长什么样？

一款 Ubuntu 下的桌面软件，**赛车仪表风格**（深色背景 + 红白配色），包含以下几个功能区域：

***

## 四大功能模块

### 1. 仪表盘（看数据）

实时显示车子的关键数据：

**数据从哪来？** 软件连接到 ROS，订阅你们已有的话题（`/yolov11/detection_image01`、`/estimation/slam/state` 等），数据自己就流进来了。

***

### 2. 参数调优面板（调参数）⭐ 核心功能

把常用的参数做成**滑块**，拖动滑块就立刻改变参数：

| 模块        | 可调参数         | 干什么用的                   |
| --------- | ------------ | ----------------------- |
| YOLO 检测   | 置信度阈值        | 越高检测越严，越少误检；越低检测越松，越少漏检 |
| IoU 融合    | 融合阈值         | 激光和相机的检测结果匹不匹配的标准       |
| 路径规划      | 预瞄距离         | 车子"看多远"，太近会抖，太远会切弯      |
| 路径规划      | 样条类型         | Catmull-Rom 严格过点，B样条更平滑 |
| 运动控制      | 速度 PID       | Kp 大响应快但容易超调，Ki 消除稳态误差  |
| 运动控制      | 最大转向角        | 限制方向盘打多少度               |
| DBSCAN 聚类 | eps / minPts | 多少个点算一堆，多远算邻居           |

- 所有参数都在静态情况下调整，调整好之后保存设置，下次启动就是新的参数

**预设保存**：调好一组参数后可以选择保存到预设，有保守模式，激进模式等，下次一键加载导入。

***

### 3. 一键启动面板（启动系统）

取代现在一行一行敲命令或者跑 bash 脚本：

- **三个大按钮**：直线加速 / 八字绕环 / 高速寻迹
- 点击后按顺序自动启动所有节点
- 实时显示每个节点的启动状态（启动中... / 运行中 ✅ / 报错 ❌）
- 可以一键全部停止杀死

**底层原理**：软件帮你执行 `roslaunch` 命令，管理每个节点的生命周期。

***

### 4. 标定工具入口（辅助功能）

在软件里一键打开：

- 相机-激光雷达联合标定工具
- IMU 内参标定
- 转向零点标定
- 查看当前加载的标定文件

***

## 用什么技术做？

### 界面框架：PySide6（Python 版 Qt）

- **为什么选它？** 用 Python 写界面，简单；和 ROS 对接方便（用 `rospy`）；可以做出好看的风格

### 和 ROS 通信

不需要把整个软件写成一个 ROS 节点。用 Python 的 `rospy` 库连接到 ROS：

- **读数据**：订阅话题，数据来了就更新界面
- **写参数**：调用 ROS 的参数服务，把滑块的值写进去
- **启动节点**：调用 `roslaunch` 命令

***

## 软件的文件结构

```
/home/a03/MyOS/
├── main.py                  # 双击运行这个就启动软件
├── config/
│   ├── theme.qss            # 颜色/字体/圆角 样式文件（CSS 语法）
│   └── presets/             # 保存的参数预设
│       ├── A模式.yaml
│       └── B模式.yaml
├── ui/
│   ├── main_window.py       # 主窗口（把所有面板拼起来）
│   ├── dashboard.py         # 仪表盘面板
│   ├── tuning_panel.py      # 参数调优面板
│   ├── launch_panel.py      # 一键启动面板
│   └── rviz_panel.py        # 嵌入 RViz 3D 可视化（可选）
├── core/
│   ├── ros_bridge.py        # 和 ROS 通信的代码
│   └── launch_manager.py    # 管理节点启动/停止
└── resources/
    ├── icons/               # 图标
    └── fonts/               # 数字字体
```

***

## 视觉风格

```
整体配色：
  背景色: 深黑 (#0a0a0f)
  面板色: 深灰蓝 (#14141f)
  主色调: 荧光绿 (#00d4aa)
  警告色: 橙色 (#ff6b35)
  文字色: 浅灰 (#e0e0e0)

风格参考：
  - 赛车 Motec 仪表盘
  - F1 方向盘显示屏
  - 暗色主题代码编辑器
```

***

## 先做什么、后做什么

| 步骤 | 做什么               | 能跑出什么效果       |
| -- | ----------------- | ------------- |
| 1  | 搭界面框架，写好 QSS 主题   | 打开软件看到一个好看的窗口 |
| 2  | 让软件能连上 ROS，读到话题数据 | 窗口里显示车速、检测数量  |
| 3  | 做出参数滑块 + 预设保存     | 拖动滑块能改参数      |
| 4  | 做出一键启动面板          | 点按钮启动整套系统     |
| 5  | 嵌入 RViz           | 在软件里看到 3D 点云  |

***

## 总结

> **一句话：做一个车队专属的"遥控器"软件，不用敲命令，不用改文件，在界面上拖动滑块就能调参数，点按钮就能启动系统。**

核心就三件事：

1. **看** — 仪表盘实时显示车辆数据
2. **调** — 拖动滑块实时调参数
3. **启动** — 一键启动/停止整套算法

